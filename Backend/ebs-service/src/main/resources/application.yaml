spring:
  application:
    name: ebs-service
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 10MB
  http:
    max-http-header-size: 10485760

server:
  port: 3500

